. strings are not fully displayable, (ie. "a\nb" displays as a\nb where \n is
    not a newline)

. hash maps
. regex
. implement format
. implement let values

. load
    - allow a list of args
    - private (not shared) variables starting in _ (ie _f, private var f)
    - prefix in

