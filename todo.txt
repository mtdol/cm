. documentation and tests for standard library
. regex
    - regex-match
    - regex-match*
    - regex-match?
    - regex-split
    - regex-replace
    - regex-replace*

    - possible form:
        -- regex {list "match" | "[ab]{2}(c+)" | "aaccf"}
    - will pretty closely follow racket regex, can point this out in documentation

. change error when file cannot be found to be more cm-like

std.cm
    -- generalized min, max

<critical>
- line number issues in pre-parse
    -- probably should just put linenumbers onto tokens directly
<critical/>

- ability to index structs by schema elem names
    >> typedef S := a,b;.
    >> def s := struct S (1,3;).
    >> -- yields 1
    >> s::"a"

- `...` match-expr operator

-- yields ((2, 3;), 1, 4)
match {list 1|2|3|4}
| a, b, ..., c; -> b, a, c
end
