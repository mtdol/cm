- testing
    -- test io
    -- test built in macros

- std.cm
    -- index_where
    -- index_of

- `...` match-expr operator
  -- yields ((2, 3;), 1, 4)
  match {list 1|2|3|4}
  | a, b, ..., c; -> b, a, c
  end
