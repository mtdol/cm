. documentation
    - regex when done
    - general look-over

. std.cm
    - tests for io/system

. regex
    - regexp-match
    - regexp-match*
    - regexp-match?
    - regexp-split
    - regexp-replace
    - regexp-replace*

    - possible form:
        -- regex {list "match" | "[ab]{2}(c+)" | "aaccf"}
    - will pretty closely follow racket regex, can point this out in documentation

<critical>
- private macros don't seem to be referencable outside of their module
- lexing failure
    -- "\"
<critical/>

- line number re-reform:
    -- change trace to show only function names

- `schemaof`
    -- returns the schema as a list of strings

- ability to index structs by schema elem names
    >> typedef S := a,b;.
    >> def s := struct S (1,3;).
    >> -- yields 1
    >> s::"a"

- `...` match-expr operator
  -- yields ((2, 3;), 1, 4)
  match {list 1|2|3|4}
  | a, b, ..., c; -> b, a, c
  end
