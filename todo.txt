- cm.rkt application includes racket stack trace, no es muy bien

- check that `file?` and `dir?` work on hidden files like `.git`

- make `ls` auto-expand paths like "~"

- make `:>` mandatory for null arg lambdas

- std.cm
    -- append_map
    -- filter_map
    -- partition
    -- shuffle
    -- sort

- testing utility
    -- work in progress
    -- add line numbers for tests being run
