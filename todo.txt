. documentation and tests for standard library
. regex
    - regex-match
    - regex-match*
    - regex-match?
    - regex-split
    - regex-replace
    - regex-replace*

    - possible form:
        -- regex {list "match" | "[ab]{2}(c+)" | "aaccf"}
    - will pretty closely follow racket regex, can point this out in documentation

. enhance cm.exe
    - allow --pkg to be used without cd'ing to the cm directory
    - --pkg
        -- -add -remove -show
        -- change install scripts into package managers

<critical>
- when lambdas from other files are called, global private variables cannot be
    referenced inside
    -- this is evident from tests/modules/private1.rkt

- line number issues in pre-parse
    -- probably should just put linenumbers onto tokens directly
<critical/>

- gensym

- generators, ie generates a value when called

- `...` match-expr operator

-- yields ((2, 3;), 1, 4)
match {list 1|2|3|4}
| a, b, ..., c; -> b, a, c
end
